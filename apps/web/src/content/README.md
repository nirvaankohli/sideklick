# Web Page Copy

Edit `site-copy.json` to change text on the public web pages.

Most copy for the home page, Pricing, Sign In, popup, navigation, and footer lives there. Keep the JSON valid: double quotes around strings, commas between items, and no comments inside the JSON file.

The React/TypeScript files still control layout, icons, images, forms, links that perform actions, and auth behavior.
