## What changed

- Restyled the quiz setup modal into clearer grouped panels for session selection, source toggles, uploaded material, and gap focus.
- Restyled the quiz-taking view with stronger cards, a cleaner results header, and a more polished explanation side panel.
- Locked `Explain Answer` until after `Check Answers` runs, then enabled the buttons and moved the longer reasoning into the side panel.

## Files touched

- `src/home.html`
- `src/home.js`
- `src/styles.css`

## Validation

- `node --check src/home.js`
